# general settings
name: train_HAT-S_d60_LLIE
model_type: HATModel
scale: 1
num_gpu: 1
manual_seed: 12345

datasets:
  train:
    name: Warwick_LLIE
    type: PairedImageDataset
    color: gray
    dataroot_gt: /root/autodl-tmp/train
    dataroot_lq: /root/autodl-tmp/LLIE/train
    meta_info: ~

    filename_tmpl: '{}'
    io_backend:
      type: disk
    gt_size: 128
    use_hflip: true
    use_rot: true
    # data loader
    num_worker_per_gpu: 8
    batch_size_per_gpu: 4
    dataset_enlarge_ratio: 50
    prefetch_mode: cuda
    pin_memory: true

  val:
    name: Warwick_LLIE
    type: PairedImageDataset
    color: gray
    dataroot_gt: /root/autodl-tmp/val
    dataroot_lq: /root/autodl-tmp/LLIE/val
    io_backend:
      type: disk


# network structures
network_g:
  type: HAT
  upscale: 1
  in_chans: 1
  img_size: 128
  window_size: 8
  compress_ratio: 3
  squeeze_factor: 30
  conv_scale: 0.01
  overlap_ratio: 0.5
  img_range: 1.
  depths: [6, 6, 6, 6, 6, 6]
  embed_dim: 60
  num_heads: [6, 6, 6, 6, 6, 6]
  mlp_ratio: 2
  upsampler: 'Denoise'
  resi_connection: '1conv'

# path
path:
  pretrain_network_g: ~
  strict_load_g: true
  resume_state: ~

# training settings
train:
  ema_decay: 0.999
  optim_g:
    type: Adam
    lr: !!float 1e-4
    weight_decay: 0
    betas: [0.9, 0.99]

  scheduler:
    type: MultiStepLR
    milestones: [150000, 300000]
    gamma: 0.5

  total_iter: 300000
  warmup_iter: -1  # no warm up

  # losses
  pixel_opt:
    type: L1Loss
    loss_weight: 1.0
    reduction: mean

# validation settings
val:
  val_freq: !!float 2e4
  save_img: false

  metrics:
    psnr: # metric name, can be arbitrary
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false
    ssim:
      type: calculate_ssim
      crop_border: 0
      test_y_channel: false


# logging settings
logger:
  print_freq: 200
  save_checkpoint_freq: !!float 2e4
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500
